# ESP-IDF build artifacts
build/
sdkconfig.old
sdkconfig
*.bin
*.elf
*.map

# IDF component manager
managed_components/
dependencies.lock

# macOS
.DS_Store

# Editor-generated backups
*.backup
*.bak
*~

# VSCode
.vscode/

# Python
__pycache__/
*.pyc
.venv/

# C test binaries
tests/test_protocol
tests/*_test

# Serial-captured logs from real hardware (NOT the committed synthetic design
# artifacts in repo-root logs/). Keep root logs/ and plots/ tracked — they are
# mockup outputs that document the schema validation.
tools/logs/*.csv
# Live host-tool session CSVs (real HW; not design mockup logs/)
logs/host/
# Accidental host CSV drop at repo root
/[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9]_[0-9][0-9][0-9][0-9][0-9][0-9].csv

# pi (coding agent) session-export artifacts — written to the project root,
# not project content.
pi-session-*.html
