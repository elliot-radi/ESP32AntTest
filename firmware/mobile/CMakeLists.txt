# ESP32AntTest — Mobile (top-level ESP-IDF project)
# See ADR-003 and docs/SPEC.md §7. Config A target: esp32c3.
cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(mobile)
